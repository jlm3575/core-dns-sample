apiVersion: v1
kind: Service
metadata:
  # any Pods with matching labels are included in this Service
  name: core-dns-sample-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
  selector:
    app: external-svc-poc
